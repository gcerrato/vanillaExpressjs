import { StackPlanSchema } from '../types.js';
export function assertIsStackPlan(config = {}) {
    const result = StackPlanSchema.safeParse(config);
    if (!result.success) {
        throw new Error('Invalid stack modification plan');
    }
}
